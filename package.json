{
  "name": "@twokeys/workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint",
    "dev": "nx run-many -t dev",
    "dev:playground": "nx run playground:dev",
    "dev:mcp": "nx run mcp-server:dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "nx": "16.10.0",
    "typescript": "^5.3.0",
    "prettier": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.0.0"
}
